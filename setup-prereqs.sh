#!/bin/bash
#set -x

#brew install socket_vmnet
#brew tap homebrew/services
#HOMEBREW=$(which brew) && sudo ${HOMEBREW} services start socket_vmnet

#brew install docker docker-compose docker-credential-helper minikube helm kubernetes-cli k9s colima
#brew services start colima
#colima start
#brew install hyperkit # hyperkit is optinal only for certian mac

# jenkins plugins
# git coverage gradle jacoco junit k8s-cli  k8s nodejs spring-config Workspace-Cleanup
